import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MapDriver = () => {
  return (
    <View>
      <Text>Map Driver Page</Text>
    </View>
  );
};

export default MapDriver;

const styles = StyleSheet.create({});
